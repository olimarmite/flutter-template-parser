name: template_parser
description: A flutter package to parse String template. It's a secure template language that is also very accessible for programmer audiences.
version: 0.0.2
homepage: https://github.com/sodipto/flutter-template-parser

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0